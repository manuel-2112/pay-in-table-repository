# Convex deployment URL (required for real-time data).
# Get this from the Convex dashboard after running `convex dev` in packages/backend,
# or from the output of `npx convex dashboard`.
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
